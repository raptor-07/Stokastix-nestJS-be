export class ConfigDto {
    botMode: string;
    asset: string;
    autoSwitch: boolean;
    trendTf: string;
    botSide: string;
    indicator: string;
    slTf: string;
    entryTf: string;
    exitTf: string;
    exitMode: string;
    alloc: number;
    numOfTrades: number;
    emaLength: number;
    targetPrice: number;
    maxLeverage: number;
    startTime: string;
    endTime: string;
}

// config.botMode,
// config.asset,
// config.autoSwitch,
// config.trendTf,
// config.botSide,
// config.indicator,
// config.slTf,
// config.entryTf,
// config.exitTf,
// config.exitMode,
// config.alloc,
// config.numOfTrades,
// config.emaLength,
// config.targetPrice,
// config.maxLeverage,
// "start",
// config.startTime,
// config.endTime